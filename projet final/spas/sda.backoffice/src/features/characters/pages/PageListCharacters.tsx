import { ListCharacters } from "../components/ListCharacters";

/**
 * Controller qui appelle le composant de listing, la recherche, ...
 */
export const PageListCharacters = () => {
    return (
        <>
            <ListCharacters></ListCharacters>
        </>
    );
}